# Quick Start

From the repository root:

```bash
python examples/run_fault_free_rbc.py
```

The script runs the built-in RBC baseline for a short fault-free example and writes outputs to `results/`.

Additional examples:

```bash
python examples/run_sensor_bias_rbc.py
python examples/run_pump_stuck_rbc.py
```

